const transactionDto = ({
  id,
  createdAt,
  updatedAt,
  label,
  value,
  type,
  categories,
  dueDate,
}) => ({
  id,
  createdAt,
  updatedAt,
  label,
  value,
  type,
  categories,
  dueDate,
})

module.exports = transactionDto
